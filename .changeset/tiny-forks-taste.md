---
"@iteraai/vue-component-inspector": minor
---

Add the functional Vue package surface with root exports plus `bridgeRuntime`, `embeddedBootstrap`, and `iterationInspector` entrypoints. This also adds mount-aware embedded bootstrap helpers so Vue apps can register explicitly and start the inspector bridge before `app.mount()`.
