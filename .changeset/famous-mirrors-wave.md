---
"@iteraai/vue-component-inspector": patch
---

Expose the Vue iteration inspector preview edit contract so it matches the React package. This adds the preview edit message and status types to the Vue public surface, validates the additive runtime capability payload, and covers the shared preview patch runtime with a Vue integration test.
