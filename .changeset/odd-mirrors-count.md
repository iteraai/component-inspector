---
"@iteraai/react-component-inspector": minor
---

Extend iteration selection payloads with a new optional, framework-neutral `componentPath` field.
Keep backward compatibility by still emitting `reactComponentPath` from the React inspector.
