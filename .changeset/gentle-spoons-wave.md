---
"@iteraai/vue-component-inspector": patch
---

Add a customer-style Vue example consumer smoke path that validates built-package imports, handshake, tree snapshots, node props, highlighting, snapshots, and iteration selection behavior. This also wires the Vue smoke fixture into the workspace scripts and CI checks so regressions in supported Vue 3 browser consumption fail clearly.
