---
"@iteraai/react-component-inspector": patch
---

Add iteration preview patch runtime support so hosts can sync and clear transient text, style, size, and asset overrides, and harden the inline devtools backend hook to fail soft when the backend module shape differs across environments.
