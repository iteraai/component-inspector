---
"@iteraai/vue-component-inspector": minor
---

Add Vue node props inspection and DOM highlight target resolution. The Vue inspector can now resolve stable node IDs back to mounted component instances, return serialized component props, and find highlightable DOM elements for DOM-rooted, fragment-backed, and nested component trees.
